# requirements.in - Source dependencies for pip-compile
# Run: pip-compile requirements.in -o requirements.txt

# Web framework
fastapi==0.110.0
uvicorn==0.27.0
pydantic>=2.7,<3.0
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0,<3.0
alembic>=1.13,<2.0
asyncpg>=0.29
psycopg2-binary>=2.9

# LLM integration
litellm>=1.40.0,<2.0
instructor>=1.0.0,<2.0
tenacity>=8.2.0,<10.0
jinja2>=3.1.0,<4.0

# Text extraction
pdfplumber>=0.10.0
pymupdf>=1.23.0
ebooklib>=0.18
python-docx>=1.1.0
pillow>=10.0.0

# TTS
edge-tts>=6.1.0

# Audio processing
numpy>=1.26.0,<3.0
soundfile>=0.12.0
ffmpeg-python>=0.2.0

# 硬质检三件套 (P0.2) — 免费CPU门禁 (DNSMOS + ASR WER + Speaker Sim)
# 缺失依赖 → 对应指标在 quality/metrics.py / audio_quality.py 诚实跳过(降级)，
# 绝不充当通过。免费资源为上限：均为开源/离线/CPU 可跑。
onnxruntime>=1.17.0               # DNSMOS 语音质量 MOS（无参考音频，免费门槛）
faster-whisper>=1.0.0             # ASR WER 字错误率（int8 CPU 模型，可懂度硬关）
# SpeechMOS / UTMOS 自然度 MOS（路线图推荐；较重，默认注释，按需启用）
# speechmos>=1.0.0
# 声纹一致性（Speaker Similarity, ECAPA-TDNN / WavLM）；需 torch (~800MB)，免费但重，
# 默认注释以守"免费资源为上限"，缺失时该指标诚实降级跳过。
# torch>=2.2.0
# speechbrain>=1.0.0

# Monitoring
langfuse>=2.0.0,<3.0
prometheus-client>=0.19.0

# OpenTelemetry
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-instrumentation-fastapi>=0.41b0
opentelemetry-instrumentation-sqlalchemy>=0.41b0
opentelemetry-instrumentation-httpx>=0.41b0
opentelemetry-instrumentation-requests>=0.41b0
opentelemetry-exporter-otlp-proto-grpc>=1.20.0
opentelemetry-exporter-prometheus>=0.41b0

# Utilities
python-dotenv>=1.0.0
python-multipart>=0.0.6
httpx>=0.27.0
pyjwt>=2.8.0
bcrypt>=4.1.0

# Documentation
mkdocs==1.5.3
mkdocs-material==9.5.13
