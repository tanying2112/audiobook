### Tests

#### Test(tts)

- fix fallback chain e2e test async probe_audio and assert_real_audio ([HEAD](https://github.com/tanying2112/AI_Lab/commit/HEAD))

  - Make probe_audio() and assert_real_audio() async to avoid asyncio.run() in event loop
  - Use ffmpeg_probe.get_duration() and get_audio_info() async APIs directly
  - All three tiers (VoxCPM2/Kokoro/Edge-TTS) now pass with real audio assertions
