### Bug Fixes

#### Fix

- P1-1 Replace datetime.utcnow() with timezone-aware datetime.now(timezone.utc) ([HEAD](https://github.com/tanying2112/AI_Lab/commit/HEAD))

  - audio_quality.py: Import timezone, use datetime.now(timezone.utc) for QualityReport.generated_at
  - sop_reflection.py: Import timezone, replace 7 occurrences of datetime.utcnow() with datetime.now(timezone.utc).isoformat().replace('+00:00', 'Z')
  - schemas/review.py: Import timezone, use default_factory=lambda: datetime.now(timezone.utc) for ReviewerJudgment.reviewed_at
  Fixes Python 3.14 DeprecationWarning for datetime.utcnow() per AUDIT_REPORT_v2.md P1-1
  - Refs: #P1-1


#### Fix

- P1-1 Replace datetime.utcnow() with timezone-aware datetime.now(timezone.utc) ([HEAD](https://github.com/tanying2112/AI_Lab/commit/HEAD))

  - audio_quality.py: Import timezone, use datetime.now(timezone.utc).isoformat().replace('+00:00', 'Z')
  - pipeline/sop_reflection.py: Import timezone, replace 7 occurrences of datetime.utcnow()
  - schemas/review.py: Use default_factory=lambda: datetime.now(timezone.utc) for reviewed_at
  Fixes Python 3.14 DeprecationWarning for datetime.utcnow().
  - Refs: docs/AUDIT_REPORT_v2.md P1-1