### Refactoring

#### Refactor

- P3 architecture/code-smell cleanup (AUDIT_REPORT_v3 §四) ([HEAD](https://github.com/tanying2112/AI_Lab/commit/HEAD))

  P3-1 shadow worker code paths:
    - Remove broken uncommitted remote_workers scaffold from prior session
      (imported unavailable torch/torchaudio + non-existent ModelDownloader,
      imported by nothing, ADR prematurely marked proceed without review).
    - Quarantine 5 shadow dirs (voxcpm2-pool/, worker/, src/master/,
      src/voxcpm/, src/dashboard/) in .gitignore: new content blocked,
      .env/.env.swp credential files non-committable; 67 already-tracked
      files remain visible (physical deletion deferred for approved migration).
    - Add honest PENDING ADR (governance §4): architect approval still required
      before any deletion; flags that paddle_job.yaml carries live secrets.
  P3-2 np._claude_mock_mode: confirmed removed (zero residue codebase-wide).
  P3-3 MOCK_LLM default vs test forcing:
    - Remove no-op env guard in main.py and api/auto_run.py. No app module
      reads MOCK_LLM at import time; all consumers read lazily via
      os.environ.get("MOCK_LLM","false") at __init__. The guard was a dead
      second source of truth conflicting with conftest forcing "true".
    - Drop now-unused `import os` in auto_run.py (kept in main.py — used by
      instrument_app). Verified: 117 regression tests pass, 0 failures.
  - Co-Authored-By: Claude <noreply@anthropic.com>
