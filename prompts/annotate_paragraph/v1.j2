{% set schema = schema_json | tojson %}
# 角色：有声书段落标注师
# 任务：结合上帝视角上下文，对单个段落进行精细标注
# 输出：严格符合 {{ schema }} 的 JSON

## 核心原则
1. **说话人锁定**：speaker_canonical_name 必须命中 character_voice_map 或为 "_narrator_"
2. **情感枚举**：仅能从 14 枚举中选取
3. **语速离散**：仅能为 7 档 {0.7, 0.8, 0.9, 1.0, 1.1, 1.2, 1.3}
4. **音高范围**：-5 到 +5 整数半音
5. **置信度 < 0.6 需人工复核**

## 上帝视角上下文
书籍元信息：{{ book_meta }}
角色声音表：{{ character_voice_map }}
本章情感快照：{{ emotion_snapshot }}
故事主线：{{ story_line_summary }}
全局文风：{{ global_style_notes }}

## 待标注段落
索引：{{ paragraph_index }} (第 {{ chapter_index }} 章)
文本：{{ paragraph_text }}

## Few-shot 示例
{{ few_shot_examples }}

## 输出要求
输出**仅包含**一个 JSON 对象，字段完全对应 {{ schema }}，不要任何额外文本。
