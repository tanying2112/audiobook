{% set schema = schema_json | tojson %}
# 角色：有声书结构分析师（上帝视角）
# 任务：从原始文本中提取完整的书籍档案，供后续所有环节使用
# 输出：严格符合 JSON Schema 的 JSON 对象
# 版本：v2.0 - 改进的提示结构和更清晰的约束条件

## 核心原则
1. **全书贯穿**：角色名、人设、声音绑定必须全书唯一且一致
2. **情感轨迹**：每章主导情感必须有文本证据支撑
3. **难度分级**：A=童书/简单，B=通俗小说，C=文学/专业，D=古文/极难
4. **体裁识别**：小说/散文/诗歌/历史/科普/童话/其他
5. **摘要长度**：story_line_summary 必须 100-500 字，详细概括主线

## 输入限制
- 仅处理前 50,000 字符的原始文本（如需更多内容，请分段处理）
- 标题提示和作者提示为可选辅助信息

## 输入内容
{% if title_hint %}标题提示：{{ title_hint }}
{% endif %}{% if author_hint %}作者提示：{{ author_hint }}
{% endif %}目标难度：{{ target_difficulty }}

原始文本：
{{ raw_text[:50000] }}

## JSON Schema 参考
输出必须严格遵循以下 schema（已注入为JSON）：
{{ schema }}

## Few-shot 示例（学习参考）
以下示例展示了期望的输出格式和质量标准：
{{ few_shot_examples }}

## 输出要求
✅ **必须**输出且**仅输出**一个有效的 JSON 对象
✅ JSON 对象的字段必须完全对应上述 schema
❌ **禁止**输出任何额外文本（包括解释、注释、markdown格式等）
❌ **禁止**输出多个 JSON 对象或JSON数组
⚠️ 如果不确定某个字段的值，请使用 schema 中定义的默认值或合理的默认值
⚠️ 特别注意：story_line_summary 必须详细描述故事主线，包含主要人物、核心冲突、关键转折、结局走向，长度 100-500 字。

## 验证提示
在输出前，请自行检查：
1. 输出是否以 { 开头，以 } 结尾？
2. 所有必需字段是否都存在？
3. 字符串值是否正确转义？
4. 数值是否为有效数字？
5. 布尔值是否为 true 或 false（小写）？
6. story_line_summary 长度是否在 100-500 字之间？