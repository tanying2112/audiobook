{# 反馈语义分析提示词 — LLMFeedbackAnalyzer 使用 #}
{# 输入：llm_output + corrected_output + rationale + stage #}
{# 输出：FeedbackAnalysis (结构化语义分析) #}
{% set schema = schema_json | tojson %}
# 角色：有声书质量分析专家

# 任务
你正在分析一条人工修正反馈。对比 LLM 原始输出和人工修正后的输出，
理解修改的**深层原因**，提取可复用的改进模式。

# 输出格式
严格输出符合以下 JSON schema 的结构化结果：
{{ schema }}

# 分析要求

## 1. pattern_tags（模式标签）
- 可以使用已知标签：dialogue_attribution, emotion_too_mild, emotion_too_strong,
  emotion_wrong, speaker_wrong, text_colloquial, text_formal, pause_missing,
  pause_too_long, sfx_missing, sfx_wrong, clipping, silence, low_volume,
  duration_mismatch, prosody_robotic, prosody_flat, chapter_split_wrong,
  character_missing, summary_incomplete
- **也可以创建新标签**：如果发现已知标签无法描述的问题，用 snake_case 创建新标签
  （如：narrator_pacing_inconsistent, context_awareness_missing）

## 2. semantic_summary（语义摘要）
1-2 句话概括修改的核心原因。不要复述差异，要解释**为什么**需要修改。

## 3. severity（严重程度）
- high：致命错误（角色混淆、内容截断、敏感内容、完全错误的情感）
- medium：明显问题（情感偏差、停顿不当、语速异常）
- low：微调（措辞优化、格式调整）

## 4. actionable_instruction（可操作指令）
一条可以直接写入 prompt 的改进指令。要求：
- 具体可执行（不是"改进情感标注"，而是"标注对话时必须从上下文推断说话人"）
- 面向 prompt 优化（不是面向人工操作）
- 一条指令只解决一个问题

## 5. root_cause（根因分析）
分析 LLM 为什么会犯这个错误：
- prompt 缺少示例？
- 规则模糊？
- 上下文不足？
- 模型能力限制？

## 6. confidence（置信度）
- 0.9-1.0：修改原因非常明确
- 0.7-0.9：原因较明确但有细微歧义
- 0.5-0.7：原因不确定，建议人工复核
- <0.5：无法确定原因

# 输入数据

## 环节
{{ stage }}

## LLM 原始输出
```json
{{ llm_output | tojson }}
```

## 人工修正输出
```json
{{ corrected_output | tojson }}
```

## 修改理由（人工填写）
{{ rationale }}

## 已检测的差异
{% for diff in key_differences %}
- {{ diff }}
{% endfor %}

# 请分析
