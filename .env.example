# Audiobook Studio — 环境变量模板
# 复制为 .env 并填入真实值：cp .env.example .env
# 严禁提交 .env 到 Git（已在 .gitignore 中）

# =============================================================================
# 数据库配置 (PostgreSQL 推荐用于生产)
# =============================================================================
# DATABASE_URL=postgresql://user:password@localhost:5432/audiobook
# SQLite 开发备选: DATABASE_URL=sqlite:///./data/audiobook.db

# =============================================================================
# 安全配置
# =============================================================================
# JWT 密钥 - 必须设置，生成命令: python scripts/generate_secrets.py --format env
# 最小 256-bit entropy (43+ URL-safe base64 字符)
JWT_SECRET_KEY=

# JWT 算法
JWT_ALGORITHM=HS256
# Token 过期时间(分钟)
ACCESS_TOKEN_EXPIRE_MINUTES=30

# =============================================================================
# LLM 提供商 API Keys (按需填入，至少配置一个)
# 优先级可在 config/llm_providers.yaml 中调整
# =============================================================================
# 默认 LLM 提供商 (用于简单场景)
LLM_PROVIDER=gemini
# OpenCode Zen (首选 OpenAI 兼容路由，极速稳定)
OPENCODE_ZEN_API_KEY=your-opencode-zen-api-key

# OpenAI (GPT-4o, GPT-4o-mini)
OPENAI_API_KEY=your-openai-api-key

# Anthropic (Claude 3.5 Sonnet, Haiku)
ANTHROPIC_API_KEY=your-anthropic-api-key

# Google Gemini (Gemini 2.0 Flash, Pro Vision)
GEMINI_API_KEY=your-gemini-api-key
# 多 Key 池支持 (可选)
# GEMINI_API_KEY_2=your-second-gemini-api-key
# GEMINI_API_KEY_3=your-third-gemini-api-key

# Groq (Llama-3.1-70B, 8B - 极快免费)
GROQ_API_KEY=your-groq-api-key

# OpenRouter (聚合多模型统一入口)
OPENROUTER_API_KEY=your-openrouter-api-key

# DeepSeek (DeepSeek-V3, Chat - 中文强)
DEEPSEEK_API_KEY=your-deepseek-api-key

# NVIDIA (Nemotron 3 Ultra)
NVIDIA_API_KEY=your-nvidia-api-key

# Cerebras (极速推理，免信用卡，1M tokens/天免费)
CEREBRAS_API_KEY=your-cerebras-api-key

# 阿里云百炼 (中文理解最强之一，100万 Token 免费)
ALIBABA_API_KEY=your-alibaba-api-key

# 智谱 AI (GLM-4-Flash 部分永久免费，中文优秀)
ZHIPU_API_KEY=your-zhipu-api-key

# 硅基流动 SiliconCloud (千万 Token 级测试包，永久额度)
SILICONCLOUD_API_KEY=your-siliconcloud-api-key

# Mistral AI (实验层最高 1B tokens/月，欧洲合规)
MISTRAL_API_KEY=your-mistral-api-key

# 百度千帆 (文心+DeepSeek+Qwen，每模型 100 万 Token)
BAIDU_API_KEY=your-baidu-api-key

# 火山引擎 (豆包大模型，500 万 Token 活动额度)
VOLCENGINE_API_KEY=your-volcengine-api-key

# 腾讯云混元 (混元全系列，100 万 Token 有效期 1 年)
TENCENT_API_KEY=your-tencent-api-key

# HuggingFace (数千开源模型，按模型限速)
HF_API_KEY=your-huggingface-api-key

# Cloudflare Workers AI (边缘推理，低延迟)
CLOUDFLARE_API_KEY=your-cloudflare-api-key
# CLOUDFLARE_ACCOUNT_ID=your-cloudflare-account-id

# GitHub Models (开发者免费，GitHub 生态集成)
GITHUB_API_KEY=your-github-api-key

# =============================================================================
# TTS 服务
# =============================================================================
# ElevenLabs (高质量商业 TTS)
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# Edge-TTS (微软免费云端 TTS，无需 key)
# 语音列表: https://github.com/rany2/edge-tts#voices

# Azure Cognitive Services TTS (免费额度: 500万字符/月)
# 申请地址: https://azure.microsoft.com/free/cognitive-services/

# =============================================================================
# Streaming TTS (首包延迟 < 500ms, 实时音频流)
# =============================================================================
# 启用流式 TTS (需要外部流式 TTS 服务: CosyVoice-Stream / Seed-TTS-Stream / MeloTTS-Stream)
ENABLE_STREAMING_TTS=false
# 流式 TTS 引擎: cosyvoice_stream / seed_tts_stream / melotts_stream
STREAMING_TTS_ENGINE=cosyvoice_stream
# 流式 TTS 服务地址
STREAMING_TTS_HOST=localhost
STREAMING_TTS_PORT=5000

# =============================================================================
# LLM 语义缓存 (启用后可节省重复/相似请求的 API 额度, <100ms 命中)
# =============================================================================
LLM_SEMANTIC_CACHE_ENABLED=true
LLM_SEMANTIC_CACHE_BACKEND=memory  # 或 redis
LLM_SEMANTIC_CACHE_SIMILARITY=0.95
LLM_SEMANTIC_CACHE_TTL=86400
LLM_SEMANTIC_CACHE_MAX_SIZE=1024
AZURE_TTS_KEY=your-azure-tts-key
AZURE_TTS_REGION=eastus
# 备用变量名兼容
# AZURE_SPEECH_KEY=your-azure-speech-key
# AZURE_SPEECH_REGION=eastus

# Google Cloud TTS (免费额度: 100万字符/月)
# 申请地址: https://cloud.google.com/text-to-speech/pricing
GOOGLE_APPLICATION_CREDENTIALS=/path/to/gcp-credentials.json
# 或设置 GCP_PROJECT_ID=your-project-id

# =============================================================================
# Remote VoxCPM2 (Cloudflare Tunnel → Kaggle GPU)
# =============================================================================
# 远程 VoxCPM2 推理端点（通过 Cloudflare Tunnel 访问 Kaggle GPU）
# 设置此变量启用真实的 Remote VoxCPM2 Port（不设置则使用 Fake Port 开发模式）
VOXCPM2_ENDPOINT=https://your-tunnel-url.example.com
# 请求总超时(秒) - 包含连接、读取、写入
VOXCPM2_TIMEOUT_SEC=60
# 最大重试次数（指数退避）
VOXCPM2_MAX_RETRIES=3
# HTTP 连接池大小（Keep-Alive 连接数）
VOXCPM2_CONNECTION_POOL_SIZE=10
# 最大保持连接数
VOXCPM2_MAX_KEEPALIVE=5

# 以下为底层细粒度控制（通常无需修改，VOXCPM2_TIMEOUT_SEC 覆盖总超时）
# 连接超时(秒)
VOXCPM2_CONNECT_TIMEOUT=5.0
# 读取超时(秒) - TTS 推理可能耗时较长
VOXCPM2_READ_TIMEOUT=120.0
# 写入超时(秒)
VOXCPM2_WRITE_TIMEOUT=30.0
# 连接池超时(秒)
VOXCPM2_POOL_TIMEOUT=10.0
# 指数退避最小等待(秒)
VOXCPM2_RETRY_MIN_WAIT=2.0
# 指数退避最大等待(秒)
VOXCPM2_RETRY_MAX_WAIT=30.0
# 熔断器失败阈值
VOXCPM2_CB_THRESHOLD=3
# 熔断器恢复超时(秒)
VOXCPM2_CB_TIMEOUT=120.0

# =============================================================================
# 文本提取服务
# =============================================================================
# 如使用云端 OCR: AWS Textract, Google Vision, Azure Form Recognizer
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_REGION=us-east-1

# =============================================================================
# 应用配置
# =============================================================================
LOG_LEVEL=INFO
PYTHONPATH=.
AUDIO_OUTPUT_DIR=./output
CHECKPOINTS_DIR=./checkpoints

# 成本控制 (单位: 美元)
COST_LIMIT_PER_CHAPTER=5.0
COST_LIMIT_PER_BOOK=20.0
COST_ALERT_THRESHOLD=0.8  # 80% 告警

# LLM 路由策略 (JSON 格式，也可用单独配置文件)
# 阶段: 模型列表(按优先序)，支持同模型多阶段
# LLM_ROUTING_CONFIG='{"extract": ["gemini-2.0-flash", "gpt-4o"], "analyze": ["gemini-2.0-flash", "claude-3.5-sonnet"], "annotate": ["groq/llama-3.1-70b", "deepseek-chat"], "edit": ["deepseek-chat", "gpt-4o"], "route": ["kokoro", "edge"], "judge": ["claude-3.5-sonnet", "gemini-2.0-flash"]}'

# =============================================================================
# 监控与可观测性 (可选)
# =============================================================================
# Langfuse (推荐：Trace + 评估 + Prompt 管理)
LANGFUSE_PUBLIC_KEY=your-langfuse-public-key
LANGFUSE_SECRET_KEY=your-langfuse-secret-key
LANGFUSE_HOST=https://cloud.langfuse.com

# Prometheus/Grafana
PROMETHEUS_ENABLED=false
GRAFANA_ENABLED=false

# =============================================================================
# 发布服务
# =============================================================================
# Audiobookshelf (有声书管理平台)
AUDIOBOOKSHELF_API_KEY=your-audiobookshelf-api-key

# =============================================================================
# 开发/调试
# =============================================================================
DEBUG=false
MOCK_LLM=false  # true 时使用模拟响应，便于离线开发
# 自我迭代 Harness 是否 mock 空跑 (C-01)。默认 true 保持既有行为;
# CI/生产设 false 使 canary/promotion gate/A-B 真实调用 LLM 产出新 prompt 版本。
SELF_ITERATION_MOCK=true

# =============================================================================
# Audio Semantic Cache (TTS 段落级缓存，支持精确/语义匹配复用音频文件)
# =============================================================================
# 启用音频语义缓存 (默认 false)
AUDIO_SEMANTIC_CACHE_ENABLED=false
# 缓存后端: memory (进程内) / redis (分布式)
AUDIO_SEMANTIC_CACHE_BACKEND=memory
# 语义相似度阈值 (0.0-1.0)，语音可接受略低阈值
AUDIO_SEMANTIC_CACHE_SIMILARITY=0.93
# 缓存 TTL (秒)，音频文件较大建议较长 TTL
AUDIO_SEMANTIC_CACHE_TTL=604800
# 最大缓存条目数 (内存限制)
AUDIO_SEMANTIC_CACHE_MAX_SIZE=512
# 嵌入提供商: hash (离线免费) / sentence_transformers (需额外依赖)
AUDIO_SEMANTIC_CACHE_EMBEDDING=hash
# 音频缓存目录 (可选，默认 ~/.cache/audiobook_studio/audio_cache)
AUDIOBOOK_STUDIO_AUDIO_CACHE=~/.cache/audiobook_studio/audio_cache
