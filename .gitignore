# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
*.egg-info/
.dist/
build/
*.egg

# Virtual environment
.venv/
env/
venv/
目录/
#/
生成/

# PyInstaller
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.py,cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# VS Code settings
.vscode/

# Logs (保留目录结构，忽略内容)
logs/*.log
!logs/.gitkeep

# Secrets
.env
*.key
*.pem
*.secret
secrets.*
inspect_secret.py
config.json

# P0-1 凭据泄露治理 (2026-07-18 docs/AUDIT_REPORT_v3.md):
# 这些脚本通常嵌入明文凭据用于 worker 初始化，绝不能进仓库。
# 如需分发，以 .env 注入 + 加密存储分发；已在历史中的泄露见 docs/RUNBOOK_rotate_credentials.md。
secrets_setup.sh
secrets_*.sh
mirror_sync.sh
bos_sync.py
*.env.swp
*.env.swo
secrets.baseline.local

# git filter-repo --replace-text 输入文件含有真实凭据泄露模式，
# 凭据轮换完成后应仅本地保留，绝不提交。
scripts/security/leaked-credential-patterns.txt

# Model weights and tokenizer files (large binary files, not tracked)
*.pth
*.safetensors
*.bin
*.gguf
*.pt
*.onnx
tokenizer_config.json
tokenizer.json
tokenizer.model
tokenization_*.py

# Large audio artifacts (products)
# Negate specific directories FIRST, then general patterns
!tests/fixtures/
!tests/fixtures/**
!storage/voice_anchors/
!storage/voice_anchors/**
*.mp3
*.wav
*.m4b

# Real LLM provider config (contains env var references, but keep template tracked)
config/llm_providers.yaml
src/audiobook_studio/config/llm_providers.yaml

# Local personalized configs with real secrets
**/config_real.py
config.json
inspect_secret.py

# Docker (不忽略 docker-compose.yml 和 Dockerfile)
# docker-compose.yml
# Dockerfile

# MkDocs site output
site/

# Misc
.DS_Store
.idea/
*.pyc

# Node modules
node_modules/

# Checkpoints (保留目录结构)
checkpoints/*
!checkpoints/.gitkeep

# Health report output
PROJECT_HEALTH.md

# IDE
*.swp
*.swo
*~

# MCP tools
.kilo/
.playwright-mcp/

# Generated audio / pipeline output
output/

# SQLite database
*.db
src/data/

# Temporary check output
check_output.txt

# Performance baselines (auto-generated)
baselines/
.aider*

# Debug/temp files
debug.py
debug_test.py
temp_fix.py
temp_*

# Coverage reports (regenerated by CI)
web/coverage/
reports/coverage_*.json

# Mock artifacts
MagicMock/

# Uploaded data
data/uploads/

# Misc temp
i
